<h1>Ajouter un tag à la plante <%= @plant.name %></h1>

<%= simple_form_for [@plant, @plant_tag] do |f| %>
  <%= f.input :tag, collection: Tag.all, input_html: {multiple: true, class: 'select2-plant-tags' }, include_hidden: false %>
  <%= f.submit %>
<% end %>
